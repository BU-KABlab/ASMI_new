# ASMI - Automated Soft Matter Indenter
# Core dependencies for CNC control, force sensing, data analysis, and visualization

# CubOS — gantry, instruments, protocol engine, data persistence.
# Temporarily pinned to an integration branch that combines both open cubos
# PRs so we can test against both before either lands on staging:
#   - PR #114 (feature/measure-gantry-injection): `measure` injects gantry
#     into closed-loop methods (e.g. ASMI.indentation) + measurement_height
#     parameter + ships deck/labware/definitions data files in the wheel.
#   - PR #116 (feature/well-depth-mm): `WellPlate.well_depth_mm` (inside
#     well depth) on the plate object so analysis pipelines can compute
#     sample thickness from `a1.z - well_depth_mm` instead of a manual
#     `well_bottom_z` knob.
# Revert to `@staging` once both PRs are merged.
cubos @ git+https://github.com/ursa-laboratories/CubOS.git@integration/asmi-test-combined

# Hardware communication
pyserial>=3.5.0

# Force sensor interface
godirect>=1.0.0

# Scientific computing
numpy>=1.21.0
scipy>=1.7.0

# Data analysis and manipulation
pandas>=1.3.0

# Visualization
matplotlib>=3.5.0

# Optional: Enhanced plotting and analysis
seaborn>=0.11.0

# Optional: Configuration management
pyyaml>=6.0

# Optional: Progress bars and CLI enhancements
tqdm>=4.62.0

# Optional: Logging enhancements
colorlog>=6.6.0

# Development dependencies (uncomment if needed)
# pytest>=6.2.0
# pytest-cov>=2.12.0
# black>=21.0.0
# flake8>=3.9.0
# sphinx>=4.0.0
