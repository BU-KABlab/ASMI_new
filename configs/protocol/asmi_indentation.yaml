# ASMI indentation protocol.
# Scans the well plate, performing step-by-step indentation at each well.
# The ASMI instrument's indentation method handles Z stepping and force reading.

protocol:
  - scan:
      plate: plate
      instrument: asmi
      method: indentation
