# ASMI board configuration — real hardware.

instruments:
  asmi:
    type: asmi
    offset_x: 0.0
    offset_y: 0.0
    depth: 0.0
    measurement_height: 0.0
    force_threshold: -100      # GoDirect device detection threshold
    sensor_channels: [1]       # GoDirect sensor channel indices
